{
  "name": "mfe-transactions",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/mfe-transactions/src",
  "prefix": "app",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "cd apps/mfe-transactions && npm run dev"
          }
        ]
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "cd apps/mfe-transactions && npm run build"
          }
        ]
      }
    }
  },
  "tags": ["type:mfe", "scope:transactions"]
}
